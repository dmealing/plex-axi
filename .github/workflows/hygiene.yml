# The one check a pull request gets on a GitHub-hosted runner.
#
# The leak scan takes seconds and it is the gate that must run before a human
# looks at a diff: this repository is public and the tool talks to a Plex Media
# Server, so a token, a server address, an absolute path into somebody's music
# collection or a real library name in a diff is the failure that matters.
# Everything heavier — lint, the Python matrix, the generated-skill check — runs
# in `ci.yml` on the maintainer's self-hosted runner, on `main` rather than on
# pull requests.
#
# That is not a reduction in rigour, and a thin-looking PR check should not be
# read as one: every change goes through the local no-mistakes gate — review,
# tests, lint, docs — before the PR exists. GitHub-hosted CI is not the primary
# quality signal here. Keep this workflow to the one cheap job; adding the
# matrix back would restore the duplicate-run waste the split exists to end.
#
# The two steps that read the pull request itself are the one exception to that,
# and they are not coverage for their own sake: they check something that exists
# ONLY on a pull request. A title and a body are in no checkout, pass under no
# hook, and can be edited after every other check has run — so `edited` is in
# the trigger list, and two separate guards depend on it being there.
#
# The first is a leak scan of the title and body. This repository is public and
# the pipeline's own document step writes into the body: it embeds an evidence
# script that assigns the checkout it ran from, and it pastes captured pytest
# output whose header carries a `rootdir:` line. Both are absolute home paths.
# That has published a home directory three times in one day across two
# repositories, with every check green each time — because nothing scanned the
# one artefact that is neither a tracked file nor a commit message. `edited` is
# what makes it work at all: the document step writes the evidence in *after*
# the pull request is opened, so a check that fired only on `opened` would pass
# the empty original body.
#
# The second is release-please's: a body that names the commit-override marker
# replaces the merged commit message with whatever follows the word, and a
# paragraph *about* the mechanism is enough to do it. It has already cost this
# repository a release. Still one job, still seconds.
name: hygiene

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read

concurrency:
  group: hygiene-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # The name names both halves deliberately: "leak check" on a job that also
  # validates the pull request body against release-please's parser is a label
  # understating what decides the outcome — the same failure mode the commit
  # checker exists to catch. A display rename changes the string a required
  # status check would reference; `main` carried no branch-protection rule when
  # this was renamed, so nothing required the old name — re-check before
  # renaming again.
  leakcheck:
    name: leak scan and commit checks
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # Prove the scanner still detects what it claims to before trusting it: a
      # scanner that quietly stopped matching would otherwise pass every diff.
      - name: Self-test against a synthetic dirty fixture
        run: python3 scripts/leakcheck.py --demo
      - name: Scan every tracked file
        run: python3 scripts/leakcheck.py
      # The surface no hook and no checkout can reach. Fails closed: without a
      # token, or on a fetch that does not answer, it fails the check rather than
      # reporting a clean it cannot support. It reports the field, line and
      # offset of a match and never the match itself — this log is public.
      - name: Scan the pull request title and body
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python3 scripts/leakcheck.py --pull-request "${{ github.event.pull_request.number }}"
      # Same discipline as the scan above: prove the checker still tells the
      # shapes apart before believing its verdict on this pull request.
      - name: Self-test the commit checker
        run: python3 scripts/commitcheck.py --demo
      - name: Check what a merge would hand release-please
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python3 scripts/commitcheck.py --pull-request "${{ github.event.pull_request.number }}"
